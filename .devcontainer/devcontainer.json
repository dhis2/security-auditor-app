// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/ubuntu
{
    "$schema": "https://raw.githubusercontent.com/devcontainers/spec/main/schemas/devContainer.schema.json",
    "name": "security-auditor-ubuntu",
    "build": {
        "dockerfile": "Dockerfile",
        "args": {
            "TZ": "${localEnv:TZ:Etc/UTC}",
            "UBUNTU_MIRROR": "http://ftp.uni-stuttgart.de/ubuntu/"
        }
    },
    "features": {
        "ghcr.io/devcontainers/features/node:1": {
            "version": "20",
            "nodeGypDependencies": true
        },
        "ghcr.io/devcontainers/features/git:1": {},
        "ghcr.io/devcontainers-extra/features/npm-package:1": {}
    },
    "containerEnv": {
        "CLAUDE_CONFIG_DIR": "/home/vscode/.claude",
        "NPM_CONFIG_IGNORE_SCRIPTS": "true",
        // yarn 1 ignores NPM_CONFIG_*, so NPM_CONFIG_IGNORE_SCRIPTS above
        // covers `npm` only. This covers yarn commands run anywhere in the
        // container, including `yarn global add` below; the repo's own
        // installs are covered by /.yarnrc, which travels with the checkout.
        "YARN_IGNORE_SCRIPTS": "true",
        "NPM_CONFIG_AUDIT": "false",
        "NPM_CONFIG_FUND": "false",
        "NPM_CONFIG_SAVE_EXACT": "true",
        "NPM_CONFIG_UPDATE_NOTIFIER": "false"
    },
    "mounts": [
        "source=security-auditor-home-vscode,target=/home/vscode,type=volume"
    ],
    "postCreateCommand": {
        "install-typescript-lsp": "npm install -g typescript-language-server || true",
        "install-yarn-and-d2": "yarn global add @dhis2/cli || true"
    },
    "postStartCommand": {
        "start-services": "bash .devcontainer/start-services.sh"
    },    
    "customizations": {
        "vscode": {
            "extensions": [
                "anthropic.claude-code",
                "eamodio.gitlens",
                "esbenp.prettier-vscode",
                "github.vscode-github-actions"
            ],
            "settings": {
                "claudeCode.allowDangerouslySkipPermissions": true,
                "claudeCode.initialPermissionMode": "bypassPermissions"
            }
        }
    }
}